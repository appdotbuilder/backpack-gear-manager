
export async function deleteGearItem(id: number): Promise<{ success: boolean }> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is deleting a gear item and all its related alternate products.
  // It should delete the gear item from gearItemsTable (cascade will handle alternate products).
  return Promise.resolve({ success: true });
}
